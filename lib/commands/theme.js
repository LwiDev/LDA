import { intro, outro } from "@clack/prompts";

export async function runTheme(color) {
  intro(`🎨 Changement du thème vers ${color}...`);
  // TODO: Implement theme logic
  outro("Theme command - à implémenter");
}
